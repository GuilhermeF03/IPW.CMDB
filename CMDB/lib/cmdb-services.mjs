import fecth from "node-fetch";
// `https://imdb-api.com/en/API/SearchMovie/${apiKey}/${name}`
// `https://imdb-api.com/en/API/Top250Movies/${apiKey}`
const apiKey = "k_iw34bd1e";

function getUserInfo(token){
    if(data[token] == undefined) Promise.reject()
  
  }

  getPopularMovies(max)
  searchMovie(req.params.movieId)
  createUser(req.body)
  createGroup(req.body)
  updateGroup(req.params.groupId, req.body)
  listUserGroups()
  deleteGroup(id)
  deleteMovie(id)
  getMovies(id)
  putMovies(id)